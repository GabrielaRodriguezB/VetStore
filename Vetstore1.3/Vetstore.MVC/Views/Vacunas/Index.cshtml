@model IEnumerable<Vetstore.Entities.Vacuna>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mascota.MascotaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ServicioClinicoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mascota.MascotaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ServicioClinicoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VacunaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VacunaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VacunaId })
        </td>
    </tr>
}

</table>
