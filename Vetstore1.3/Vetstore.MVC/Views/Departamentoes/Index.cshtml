@model IEnumerable<Vetstore.Entities.Departamento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ubigeo.UbigeoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UbigeoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ubigeo.UbigeoId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UbigeoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepartamentoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepartamentoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepartamentoId })
        </td>
    </tr>
}

</table>
