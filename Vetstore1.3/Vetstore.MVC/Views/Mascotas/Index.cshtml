@model IEnumerable<Vetstore.Entities.Mascota>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.ClienteId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.ClienteId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MascotaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MascotaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MascotaId })
        </td>
    </tr>
}

</table>
