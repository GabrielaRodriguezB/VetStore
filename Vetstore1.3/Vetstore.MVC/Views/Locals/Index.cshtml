@model IEnumerable<Vetstore.Entities.Local>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Direccion.DireccionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DireccionId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion.DireccionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DireccionId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LocalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LocalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LocalId })
        </td>
    </tr>
}

</table>
